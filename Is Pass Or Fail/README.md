## Write a function to check if a student has passed or failed.

````

### Instructions

---

Suppose, the mark is 40.
If the mark is greater than or equal to 40, return `"Pass"`. Otherwise, return `"Fail"`.```

````

```

### Example

---

For this input
`marks = 45`
then result should be:
`"Pass"`

```
