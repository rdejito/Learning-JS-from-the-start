## Write a function to check if a number is negative.

### Instructions

If the number is less than 0, retun `"Negative"`. Otherwise, return `"Not Negative"`.

### Example

For this input

```
num = -4
```

the result should be:

```
"Negative"
```

**Reason: -4** is less than 0, so it's a negative number.
