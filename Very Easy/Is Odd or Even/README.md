## Write a function to check if a number is odd or even.

### Instructions

If the number is even, return `"Even"`. If the number is odd, return `"Odd"`.

### Example

For this input

```
num = 4
```

the result should be:

```
"Even"
```

**Reason: 4** is even so the result is `"Even"`.
