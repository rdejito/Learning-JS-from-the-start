## Write a function to find the longest consecutive zeros in a binary string.

### Instructions

Return the longest consecutive zeros in the binary string `str`.

### Example

For this input

```
str = "1010010001"
```

the result should be:

```
3
```

**Reason:** The longest sequence of consecutive zeros in the binary string `"1010010001"` is **3**.
